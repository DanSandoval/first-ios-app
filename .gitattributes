# Authored on Windows, built on a macOS runner. Force LF in the repository for
# everything the build touches, so shell bodies inside the workflow and the
# Swift sources never reach the runner with CRLF line endings.
* text=auto eol=lf

*.png binary
